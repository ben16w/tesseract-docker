---

services:
  homepage:
    image: "git.tepig.${TESSERACT_DOMAIN}/tesseract/homepage:latest"
    container_name: homepage
    restart: always
    ports:
      - 8866:80
    environment:
      HOMEPAGE_TITLE: Welney.net
      HOMEPAGE_DOMAIN: welney.net
