---
version: "2.1"

services:
  navidrome:
    image: deluan/navidrome:latest
    container_name: navidrome
    user: "${DOCKER_UID}:${DOCKER_GID}"
    restart: always
    ports:
      - "4533:4533"
    environment:
      ND_MUSICFOLDER: "/music"
      ND_DATAFOLDER: "/data"
      ND_SCANINTERVAL: "12h"
      ND_LOGLEVEL: "info"
      ND_PORT: 4533
      ND_TRANSCODINGCACHESIZE: "100MB"
      ND_SESSIONTIMEOUT: "24h"
      ND_DEFAULTTHEME: "Spotify-ish"
      ND_SPOTIFY_ID: "${DOCKER_SPOTIFY_ID}"
      ND_SPOTIFY_SECRET: "${DOCKER_SPOTIFY_SECRET}"
    volumes:
      - "${DOCKER_APPDATA_PATH}/navidrome_data:/data"
      - "${DOCKER_DATA_PATH}/Music:/music:ro"
