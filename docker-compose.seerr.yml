---

services:
  seerr:
    image: ghcr.io/seerr-team/seerr:latest
    container_name: seerr
    restart: always
    init: true
    ports:
      - 5055:5055
    environment:
      LOG_LEVEL: debug
      TZ: "${TESSERACT_TZ}"
    volumes:
      - "${TESSERACT_APPDATA_PATH}/seerr_config:/app/config"
