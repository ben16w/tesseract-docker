---
version: '2.1'

services:
  jenkins:
    container_name: jenkins
    image: jenkins/jenkins:lts-jdk11
    user: root
    ports:
      - 8083:8080
      - 50000:50000
    volumes:
      - ${TES_APPDATA_PATH}/jenkins_config:/var/jenkins_home
      - /var/run/docker.sock:/var/run/docker.sock